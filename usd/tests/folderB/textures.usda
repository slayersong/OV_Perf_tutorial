#usda 1.0

def Material "matB1"
{
    token outputs:surface.connect = </matB1/pbrMat.outputs:surface>

    def Shader "pbrMat"
    {
        uniform token info:id = "UsdPreviewSurface"
        color3f  inputs:diffuseColor.connect = </matB1/baseColorTex.outputs:rgb>
        token outputs:surface
    }

    def Shader "baseColorTex"
    {
        uniform token info:id = "UsdUVTexture"
        asset inputs:file = @asset1.txt@
        float3 outputs:rgb
    }

    def Shader "PrimvarSt"
    {
        uniform token info:id = "UsdPrimvarReader_float2"
        string inputs:varname = "st"
        float2 outputs:result
    }
}

def Material "matB2"
{
    token outputs:surface.connect = </matB2/pbrMat.outputs:surface>

    def Shader "pbrMat"
    {
        uniform token info:id = "UsdPreviewSurface"
        color3f  inputs:diffuseColor.connect = </matB2/baseColorTex.outputs:rgb>
        token outputs:surface
    }

    def Shader "baseColorTex"
    {
        uniform token info:id = "UsdUVTexture"
        asset inputs:file = @asset2.txt@
        float3 outputs:rgb
    }

    def Shader "PrimvarSt"
    {
        uniform token info:id = "UsdPrimvarReader_float2"
        string inputs:varname = "st"
        float2 outputs:result
    }
}

def Material "matB3"
{
    token outputs:surface.connect = </matB3/pbrMat.outputs:surface>

    def Shader "pbrMat"
    {
        uniform token info:id = "UsdPreviewSurface"
        color3f  inputs:diffuseColor.connect = </matB3/baseColorTex.outputs:rgb>
        token outputs:surface
    }

    def Shader "baseColorTex"
    {
        uniform token info:id = "UsdUVTexture"
        asset inputs:file = @asset1.txt@
        float3 outputs:rgb
    }

    def Shader "PrimvarSt"
    {
        uniform token info:id = "UsdPrimvarReader_float2"
        string inputs:varname = "st"
        float2 outputs:result
    }
}

def Material "matOmniB"
{
    token outputs:surface.connect = </matOmniB/pbrMat.outputs:surface>

    def Shader "pbrMat"
    {
        uniform token info:id = "UsdPreviewSurface"
        color3f  inputs:diffuseColor.connect = </matOmniB/baseColorTex.outputs:rgb>
        token outputs:surface
    }

    def Shader "baseColorTex"
    {
        uniform token info:id = "UsdUVTexture"
        asset inputs:file = @omniverse://my_texture.png@
        float3 outputs:rgb
    }

    def Shader "PrimvarSt"
    {
        uniform token info:id = "UsdPrimvarReader_float2"
        string inputs:varname = "st"
        float2 outputs:result
    }
}

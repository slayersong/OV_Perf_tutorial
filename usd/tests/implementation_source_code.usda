#usda 1.0

def Material "mat_1" {
    token outputs:surface.connect = </mat_1/shader.outputs:surface>

    def Shader "shader" {
        uniform token info:implementationSource = "sourceCode"
        uniform string info:sourceCode = "Hello!"
        token outputs:surface
    }
}

def Material "mat_2" {
    token outputs:surface.connect = </mat_2/shader.outputs:surface>

    def Shader "shader" {
        uniform token info:implementationSource = "sourceCode"
        uniform string info:sourceCode = "Hello!"
        token outputs:surface
    }
}

def Material "mat_3" {
    token outputs:surface.connect = </mat_3/shader.outputs:surface>

    def Shader "shader" {
        uniform token info:implementationSource = "sourceCode"
        uniform string info:sourceCode = "Hello!"
        uniform string info:something:sourceCode = "Hello there."
        token outputs:surface
    }
}

def Material "mat_4" {
    token outputs:surface.connect = </mat_4/shader.outputs:surface>

    def Shader "shader" {
        uniform token info:implementationSource = "sourceCode"
        uniform string info:something:sourceCode = "Hello there."
        token outputs:surface
    }
}

def Material "mat_5" {
    token outputs:surface.connect = </mat_5/shader.outputs:surface>

    def Shader "shader" {
        uniform token info:implementationSource = "sourceCode"
        uniform string info:something:sourceCode = "Hello there."
        token outputs:surface
    }
}
